apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: sentiment-pvc
spec:
  storageClassName: ibmc-vpc-block-5iops-tier
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 10Gi
